{
  "name": "@armada/shell",
  "private": true,
  "version": "0.0.0",
  "description": "The application's layout: the rail, the head, the status bar, and what catches a failure. Holds a screen; is not one.",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@armada/components": "workspace:*",
    "@armada/protocol": "workspace:*",
    "@armada/tokens": "workspace:*",
    "lucide-react": "^0.550.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "typescript": "^5.9.0"
  }
}

# packages/

Shared TypeScript that more than one app needs — the generated IPC types, and
whatever else Bridge and a future surface both read. Empty until something is
actually shared: a package created before it has a second consumer is a folder
with an import path.

// GENERATED by `pnpm --filter @armada/desktop codegen`. Do not hand-edit.
//
// Every act Bridge offers: what it is called, what it is bound to, and the
// glyph it draws — or why it draws none. From
// `crates/core-model/domain/actions.toml`, which is the artifact
// `docs/contracts/design-system.md` promises under "One artifact, three
// columns" and the authority on all three.
//
// **A Motion is here and is not an act.** `move_focus`, `open_focused` and
// `focus_chapter` move the cursor and act on nothing; the registry says they
// appear in no palette and carry no glyph, so they are emitted for
// completeness and filtered out by anything that draws a list of acts.
//
// **A blank glyph is a fact, not a default.** `iconAbsent` says which kind of
// blank it is: `undecided` means no registered silhouette means the act and
// assigning one is a decision for `packages/icons/icons.toml`; `by design`
// means a document decided the act carries none. A surface says which it is
// drawing rather than inventing a glyph to fill the column.
//
// **`unbuilt` names the issue that answers the key.** The registry is ahead
// of the app deliberately, because the map was settled by drawing. A palette
// that displays a binding beside every entry would otherwise offer a row a
// person presses and gets nothing from, which is worse than one that is
// absent.

import { ChevronRight, CornerUpRight, Eye, FileDiff, Filter, MessageSquare, Power, RotateCw, Search, Stamp, Terminal, X } from "lucide-react";
import type { LucideIcon } from "lucide-react";

/** Whether the row is an act or a movement of the cursor. */
export type ActionKind = "Action" | "Motion";

/** Modifier-based and working anywhere, or single-key and on what is focused. */
export type ActionTier = "Global" | "Contextual";

/**
 * Where the binding is offered. The registry's own set, spelled the registry's
 * way, and read off the rows rather than declared — so a scope that appears
 * in `actions.toml` appears here and nowhere else has to be told.
 */
export type ActionScope =
  | "anywhere"
  | "detail"
  | "dispatch card"
  | "job board"
  | "list"
  | "list and detail"
  | "piloted job";

/** Why an act's glyph column is empty. `null` where it is not. */
export type IconAbsence = "undecided" | "by design";

export type Action = {
  /** The id an implementation binds to. The registry's table key. */
  readonly id: string;
  readonly kind: ActionKind;
  readonly tier: ActionTier;
  /** What a person reads, in the lexicon's word. Never the id. */
  readonly verb: string;
  /** The glyph, or `null` — in which case `iconAbsent` says why. */
  readonly icon: LucideIcon | null;
  readonly iconAbsent: IconAbsence | null;
  /** The binding, spelled as the contract's map spells it. */
  readonly shortcut: string;
  readonly scope: ActionScope;
  readonly destructive: boolean;
  readonly confirms: boolean;
  /** The issue that gives the binding an act, on a row nothing answers yet. */
  readonly unbuilt: string | null;
};

/**
 * An issue reference, composed rather than written.
 *
 * **Because the design gate reads a short issue number as a colour literal.**
 * Three hex digits behind a hash is exactly what a hex colour looks like, and
 * the rule that keeps raw hex out of a renderer cannot tell one from the
 * other. Writing the number apart from the hash is the smaller answer; opting
 * this file out of that rule would switch off colour checking across a file
 * of glyphs. **The underlying fix is #356**, and this helper goes when it
 * lands.
 */
function issue(n: number): string {
  return `#${n}`;
}

/**
 * The map, in the registry's order: global first, then contextual.
 *
 * Order is load-bearing in one place only — the palette groups by section and
 * keeps registry order inside each — so nothing here is sorted.
 */
export const ACTIONS: readonly Action[] = [
  {
    id: "command_palette",
    kind: "Action",
    tier: "Global",
    verb: "Command palette",
    icon: null,
    iconAbsent: "undecided",
    shortcut: "⌘K",
    scope: "anywhere",
    destructive: false,
    confirms: false,
    unbuilt: null,
  },
  {
    id: "bridge_surfaces",
    kind: "Action",
    tier: "Global",
    verb: "Bridge surfaces",
    icon: null,
    iconAbsent: "by design",
    shortcut: "⌘1–⌘4",
    scope: "anywhere",
    destructive: false,
    confirms: false,
    unbuilt: null,
  },
  {
    id: "helm",
    kind: "Action",
    tier: "Global",
    verb: "Helm",
    icon: MessageSquare,
    iconAbsent: null,
    shortcut: "⌘5",
    scope: "anywhere",
    destructive: false,
    confirms: false,
    unbuilt: null,
  },
  {
    id: "toggle_sidebar",
    kind: "Action",
    tier: "Global",
    verb: "Toggle sidebar",
    icon: null,
    iconAbsent: "undecided",
    shortcut: "⌘\\",
    scope: "anywhere",
    destructive: false,
    confirms: false,
    unbuilt: null,
  },
  {
    id: "history",
    kind: "Action",
    tier: "Global",
    verb: "Back / forward",
    icon: null,
    iconAbsent: "undecided",
    shortcut: "⌘[ ⌘]",
    scope: "anywhere",
    destructive: false,
    confirms: false,
    unbuilt: null,
  },
  {
    id: "close",
    kind: "Action",
    tier: "Global",
    verb: "Close",
    icon: X,
    iconAbsent: null,
    shortcut: "Esc",
    scope: "anywhere",
    destructive: false,
    confirms: false,
    unbuilt: null,
  },
  {
    id: "move_focus",
    kind: "Motion",
    tier: "Contextual",
    verb: "Move focus",
    icon: null,
    iconAbsent: null,
    shortcut: "j / k / ↓ / ↑",
    scope: "list and detail",
    destructive: false,
    confirms: false,
    unbuilt: null,
  },
  {
    id: "open_focused",
    kind: "Motion",
    tier: "Contextual",
    verb: "Open the focused job",
    icon: null,
    iconAbsent: null,
    shortcut: "Enter",
    scope: "list",
    destructive: false,
    confirms: false,
    unbuilt: null,
  },
  {
    id: "open",
    kind: "Action",
    tier: "Contextual",
    verb: "Open",
    icon: null,
    iconAbsent: "undecided",
    shortcut: "o",
    scope: "list",
    destructive: false,
    confirms: false,
    unbuilt: null,
  },
  {
    id: "review",
    kind: "Action",
    tier: "Contextual",
    verb: "Review",
    icon: Eye,
    iconAbsent: null,
    shortcut: "r",
    scope: "list and detail",
    destructive: false,
    confirms: false,
    unbuilt: null,
  },
  {
    id: "attest",
    kind: "Action",
    tier: "Contextual",
    verb: "Attest",
    icon: Stamp,
    iconAbsent: null,
    shortcut: "t",
    scope: "list and detail",
    destructive: false,
    confirms: false,
    unbuilt: null,
  },
  {
    id: "redirect",
    kind: "Action",
    tier: "Contextual",
    verb: "Redirect",
    icon: CornerUpRight,
    iconAbsent: null,
    shortcut: "d",
    scope: "list and detail",
    destructive: false,
    confirms: false,
    unbuilt: null,
  },
  {
    id: "approve",
    kind: "Action",
    tier: "Contextual",
    verb: "Approve",
    icon: null,
    iconAbsent: "undecided",
    shortcut: "a",
    scope: "dispatch card",
    destructive: false,
    confirms: false,
    unbuilt: null,
  },
  {
    id: "restart_step",
    kind: "Action",
    tier: "Contextual",
    verb: "Restart step",
    icon: null,
    iconAbsent: "undecided",
    shortcut: "s",
    scope: "detail",
    destructive: false,
    confirms: false,
    unbuilt: null,
  },
  {
    id: "pilot",
    kind: "Action",
    tier: "Contextual",
    verb: "Pilot",
    icon: Terminal,
    iconAbsent: null,
    shortcut: "p",
    scope: "list and detail",
    destructive: false,
    confirms: false,
    unbuilt: issue(250),
  },
  {
    id: "copy_debug_info",
    kind: "Action",
    tier: "Contextual",
    verb: "Copy debug info",
    icon: null,
    iconAbsent: "by design",
    shortcut: "c",
    scope: "list and detail",
    destructive: false,
    confirms: false,
    unbuilt: null,
  },
  {
    id: "report_job",
    kind: "Action",
    tier: "Contextual",
    verb: "Report this job",
    icon: null,
    iconAbsent: "undecided",
    shortcut: "b",
    scope: "detail",
    destructive: false,
    confirms: true,
    unbuilt: null,
  },
  {
    id: "kill",
    kind: "Action",
    tier: "Contextual",
    verb: "Kill",
    icon: Power,
    iconAbsent: null,
    shortcut: "x",
    scope: "list and detail",
    destructive: true,
    confirms: true,
    unbuilt: null,
  },
  {
    id: "kill_and_redispatch",
    kind: "Action",
    tier: "Contextual",
    verb: "Kill & redispatch",
    icon: null,
    iconAbsent: "undecided",
    shortcut: "X",
    scope: "detail",
    destructive: true,
    confirms: true,
    unbuilt: issue(291),
  },
  {
    id: "new_job",
    kind: "Action",
    tier: "Contextual",
    verb: "New job",
    icon: null,
    iconAbsent: "undecided",
    shortcut: "n",
    scope: "anywhere",
    destructive: false,
    confirms: false,
    unbuilt: null,
  },
  {
    id: "search",
    kind: "Action",
    tier: "Contextual",
    verb: "Search",
    icon: Search,
    iconAbsent: null,
    shortcut: "/",
    scope: "list",
    destructive: false,
    confirms: false,
    unbuilt: null,
  },
  {
    id: "state_filter",
    kind: "Action",
    tier: "Contextual",
    verb: "State filter",
    icon: Filter,
    iconAbsent: null,
    shortcut: "1–5",
    scope: "job board",
    destructive: false,
    confirms: false,
    unbuilt: null,
  },
  {
    id: "observe",
    kind: "Action",
    tier: "Contextual",
    verb: "Observe",
    icon: null,
    iconAbsent: "undecided",
    shortcut: "v",
    scope: "detail",
    destructive: false,
    confirms: false,
    unbuilt: null,
  },
  {
    id: "submit_for_verification",
    kind: "Action",
    tier: "Contextual",
    verb: "Submit for verification",
    icon: null,
    iconAbsent: "undecided",
    shortcut: "u",
    scope: "piloted job",
    destructive: false,
    confirms: false,
    unbuilt: null,
  },
  {
    id: "redispatch",
    kind: "Action",
    tier: "Contextual",
    verb: "Redispatch as a new job",
    icon: RotateCw,
    iconAbsent: null,
    shortcut: "e",
    scope: "list and detail",
    destructive: false,
    confirms: false,
    unbuilt: null,
  },
  {
    id: "disclose",
    kind: "Action",
    tier: "Contextual",
    verb: "Expand and collapse",
    icon: ChevronRight,
    iconAbsent: null,
    shortcut: "h / l / ← / →",
    scope: "detail",
    destructive: false,
    confirms: false,
    unbuilt: null,
  },
  {
    id: "focus_chapter",
    kind: "Motion",
    tier: "Contextual",
    verb: "Move between chapters",
    icon: null,
    iconAbsent: null,
    shortcut: "[ ]",
    scope: "detail",
    destructive: false,
    confirms: false,
    unbuilt: null,
  },
  {
    id: "open_log",
    kind: "Action",
    tier: "Contextual",
    verb: "Open the log",
    icon: null,
    iconAbsent: "undecided",
    shortcut: "Enter",
    scope: "detail",
    destructive: false,
    confirms: false,
    unbuilt: null,
  },
  {
    id: "open_diff",
    kind: "Action",
    tier: "Contextual",
    verb: "Open the diff",
    icon: FileDiff,
    iconAbsent: null,
    shortcut: "f",
    scope: "detail",
    destructive: false,
    confirms: false,
    unbuilt: null,
  },
  {
    id: "open_stage",
    kind: "Action",
    tier: "Contextual",
    verb: "Open the stage",
    icon: null,
    iconAbsent: "undecided",
    shortcut: "g",
    scope: "detail",
    destructive: false,
    confirms: false,
    unbuilt: null,
  },
];

/**
 * One act, by the id an implementation binds to.
 *
 * `| undefined` because a caller may ask for an id this build has never
 * heard of, and a missing act is a real answer rather than a crash.
 */
export const ACTION: Readonly<Record<string, Action | undefined>> = Object.fromEntries(
  ACTIONS.map((action) => [action.id, action]),
);

---
skill: reflect
verdict: PASS
date: 2026-09-12T21:30:00Z
---

A refactor Job finished `completed_success` with its work on a local branch that nobody pushed. Its Drone had run `git commit` itself, told to by a v1 user-level skill ("Commit before you finish"), and the operator's inherited settings allowed it. Fleet's delivering step then found a clean tree and pushed only commits it had made, so it sent nothing and logged "held nothing new". Three fixes landed: #779 delivered that Job's work by hand, #783 delivers any branch ahead of its base whoever committed it, and #786 denies Drones the git verbs that change a repository. At the owner's choice, six v1 user-level skills from Aug 17 were deleted from the operator's machine, including the two that told a Drone to commit (`write-plan`, `write-design`). Any Drone that loaded them would have been refused since #786.

## Follow-ups
| Action | Owner | Detail |
|---|---|---|
| Restart Fleet on a rebuilt binary | Owner | The running Fleet started at 17:58Z, before #783 (19:49Z) and #786 (21:14Z) merged, so neither fix is live yet. |
| Close the git-block gaps, if wanted | Owner | A bare `git branch <name>` still creates a branch, and git's `-c` and `--namespace` flags can still move the verb out of position. Both are named in `crates/adapters/src/git_guard.rs`. |
| Decide whether a Check inherits ambient `ARMADA_PORT_*` | Owner | #779 changed a test so it no longer reads the machine it runs on. It did not decide whether that inheritance is right. |
| Watch the next delivering Job | Owner | #783 has not run against a real remote, and the path that delivers again after a conflict is resolved is untested. |

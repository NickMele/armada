---
skill: reflect
verdict: PASS
date: 2026-09-13T16:36:54Z
milestone: Board
---

Always allow now saves a rule the person picks (#834, #853), and Fleet keeps it per Manifest instead of committing it to `armada.yml` (#836, #868). The Board shows Job handles, not ULIDs (#882, #884). Two follow-ups found along the way were fixed and merged: the leaky adopting tests (#890, #911) and the `connection.ts` split (#891, #909). Stale prose about Always allow was corrected, and the reflect skill now says a baseline worktree needs its own target directory (#892). Fleet and Bridge were restarted onto main.

The protocol minor collided on three rebases. Twice both sides had made the same bump, so git applied it silently and the branch had to be renumbered by hand, the last time to 13.7.

A corrupted pnpm install, one bad byte written on Sep 2, broke every `pnpm` Check partway through. It was restored from a download verified against the registry checksum. Why it surfaced only on 13 Sep is unexplained.

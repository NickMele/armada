---
skill: reflect
verdict: FAIL
date: 2026-09-17T12:05:55Z
---

The motion, colour and type handoff landed in full: decisions in #1218, then #1219–#1222, #1227–#1229 and #1232–#1234. #1227 left `main` red until #1229, and `work-issue` now names that. FAIL because one owner action is left: an agent may not edit its own permissions.

## Follow-ups
| Action | Owner | Detail |
|---|---|---|
| Commit or discard the `gh pr merge` allow entry | Owner | Uncommitted in `.claude/settings.json` on the main checkout, written as `"gh pr merge"`, which matches nothing. The working form is `Bash(gh pr merge *)`. Blocked for the agent as self-modification |
| Decide whether change marks are capped on a busy Board | Owner | `[decay-cap]` in `docs/contracts/design-system.md` |
| Test that a button's answer line clears | Unassigned | #1237 |
| Keep the age in a changed row's note on a narrow Board | Unassigned | #1238 |

Dropped by the owner, recorded so they are not raised again: every Job that changed while Bridge was disconnected is marked "just changed" on reconnect; the press line opens on every button, Fleet-bound or not; DroneBrief section headings share the brief title's weight; a row moving between Overview panels does not travel.

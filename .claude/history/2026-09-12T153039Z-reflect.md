---
skill: reflect
verdict: PASS
date: 2026-09-12T15:30:39Z
---

A step's live Checks and Judge were built and unreadable: the turn held the Job's slot across its gate and across its convergence look, and `get_job` and `list_jobs` read that same slot. Both holds were measured against a running Fleet, fixed in #739 and #744, and each carries a test that fails against the code it fixes.

## Follow-ups
| Action | Owner | Detail |
|---|---|---|
| Restart Fleet | Owner | Neither fix is live until the binary is rebuilt. The wire also moved 11.4 → 11.5 on somebody else's merge while the Fleet was up. It has since stopped on its own, so the next start picks both up. |
| Attribute the 1–3s read stalls | https://github.com/NickMele/armada/issues/755 | The footprint's git read (10–20 ms), machine load (63 ms median under a full build) and a per-Job slot are all excluded by measurement. `admit_next` holding the roster lock across a whole dispatch is the unverified reading; the issue asks for the instrument before the fix. |

---
skill: reflect
verdict: PASS
date: 2026-09-01T20:30:00Z
milestone: Board
---

Six issues merged and closed, the issue-writing practice landed with a skill to
apply it, and the comparator was retired out from under two of the day's own
findings.

## What this session's work made untrue, and who holds it

Every reference to `shoot` — `docs/practices/comparing-to-the-drawing.md`, its
`docs/INDEX.md` entry, `.claude/skills/armada-local/SKILL.md` and
`apps/desktop/screens/build.mjs` — is inside the working tree of the agent
retiring it, which had already deleted `build.mjs` when this ran. Left alone
deliberately: reconciling prose another agent is mid-way through rewriting is
how two agents produce one conflict.

`docs/instructions/claude-design.md` and roughly fourteen other files name a
design project that is being replaced. The owner's call was to leave them for
whoever builds the replacement rather than file a sweep, on the grounds that
they will hit the files anyway.

## What the day cost, stated once

Three agents in one day compared a build against a **cached** drawing and two
were right by luck. The comparison could not vouch for its own design half, and
nobody noticed until a drawing changed underneath it. Separately, the escalated
badge pair was green while both halves came from the same wrong fixture — a
gallery fixture that imitates the app in React, and the only place a human
retypes anything. Neither failure was in the translation from design to code.
Both were in the third artifact sitting between them.

## Follow-ups

| Action | Owner | Detail |
|---|---|---|
| Finish Board | Human | A new agent, once the in-house design system is in place. #110 is unblocked — #246 took the protocol to 6.4, so its wire change has a clear run |
| Board's four remaining | Human | #228, #187, #209, #249 all wait on either a drawing read or a decision. #249's is the sharpest: do adopted learnings land in the Job's own PR or a new one |
| `armada/retire-the-comparator` | Claude Code | Another agent's branch, uncommitted in the shared checkout. Not this session's to merge |

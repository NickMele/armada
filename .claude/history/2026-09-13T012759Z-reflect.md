---
skill: reflect
verdict: FAIL
date: 2026-09-13T01:27:59Z
---

The board's Clear crashed the window on `null.slice`: Fleet sent a reclaim's empty fields as `null` and Bridge compared against `undefined`. Fleet stopped sending them (#827) and Bridge learned to read them (#832). The same scan found two more fields sent as `null` — a flag drawing `file:null` and an argument offering the wrong button — fixed here, with a foundations rule that refuses the next one. Fleet runs #827; Bridge was not restarted onto #832 because a Drone was working, and the owner chose to restart it from Bridge later.

## Follow-ups
| Action | Owner | Detail |
|---|---|---|
| Restart Fleet and Bridge onto `main` | Human | Bridge's restart button, which refuses only while a Drone is working. It picks up #832 and this change's two fields together |

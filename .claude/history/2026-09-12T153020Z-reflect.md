---
skill: reflect
verdict: FAIL
date: 2026-09-12T15:30:20Z
---

Two pull requests landed the stale-diff fix and three coherence gaps behind it
— #730 and #734 — and this pass reconciled four places that still described the
patch as a read nobody takes twice. One follow-up has no home yet.

## Follow-ups
| Action | Owner | Detail |
|---|---|---|
| Decide where the diff sheet's content staleness lives | owner | The sheet re-reads when the file list moves; Fleet republishes a footprint only where the list changed, so a Drone editing the same files with the sheet open draws the patch as of the last time the list moved. Named in #730's body and in `produced.ts`, neither of which will raise it again. An open question in `docs/journeys/monitor-active-work.md` or an issue — asked in the terminal, unanswered |
| Add a second symptom to #747, or leave it | owner | `packages/screens/src/run-sheet.test.tsx` timed out at 15s in one full `bridge-test` of three, passing alone. #747 is the same cause — the machine under load — and a different symptom |

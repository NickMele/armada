---
skill: reflect
verdict: PASS
date: 2026-09-18T03:30:00Z
---

A Job killed on the belief that its second step had restarted its first turned
out to be re-deriving file paths the first step had already recorded, and four
changes came out of it: the plan's task detail reaches the next step (#1417),
the unwritable-rule refusal says what a Drone can actually do (#1417), the
feature workflow's first step is called `plan` like every other workflow's
(#1425), and a plan task carries its files and both ends of its evidence at
protocol 15.0 (#1427). `docs/concepts/plan.md` described a task as a title and
an optional detail and was corrected here.

Two mistakes worth keeping. A summary that summed `test result:` fields by
position counted every `FAILED.` line as zero failures, so three red tests in
`crates/ipc/src/tests/planning.rs` were reported as 2,939 passing; the merge
line caught them. And three CSS tokens were invented (`--space-3xs`,
`--space-2xs`, `--radius-xs`) before the real scale was checked.

---
skill: reflect
verdict: PASS
date: 2026-09-18T19:45:00Z
---

Closed the session that replaced a Drone's refused check-runner command with the
Check's own, landed the last branch, and measured the one claim that had been
shipped unproven.

Five branches went in: #1496 dropped a warning that had reached `main`, #1502
made a refusal name the check a command actually looks like, #1510 and #1516
were the two shapes of the answer — the second superseding the first after the
owner refused a denial whose body was a passing test report — and #1517 gave the
transcript one waiting reader. #1504 carried the last docs.

**`updatedInput` was documented and never exercised here.** Every path in Armada
handed the same input back, so #1516 shipped on a harness behaviour nothing in
the repository or in spike 15 had tested. Measured on 2026-09-18 against Claude
Code 2.1.277: the asked command never runs, the substitute does, and the agent
says unprompted that a different command ran. Recorded as `stdio-substitute` in
spike 15, and the contract now cites it rather than asserting it.

A first attempt at that measurement used `echo` on both sides and proved
nothing — a read-only command never reaches the permission tool, so the CLI ran
the asked command without consulting the stub at all. The spike already said so
in its Provenance section and I read it afterwards rather than before.

Two follow-ups the work created were filed rather than left in a PR body:
https://github.com/NickMele/armada/issues/1521 for the Manifest Proposer the
runner adapter's detection was built for, and
https://github.com/NickMele/armada/issues/1523 for a substituted command running
outside the machine's `checks-at-once` bound — the half of #1444 that closing it
did not cover.

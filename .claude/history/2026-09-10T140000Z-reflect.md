---
skill: reflect
verdict: PASS
date: 2026-09-10T14:00:00Z
---

Tightened the TypeScript compiler across all six projects — four unused-code
checks and `noUncheckedIndexedAccess` — and fixed the 135 errors they found.
Merged as #590. One claim in that PR was wrong and is corrected on the PR
itself: `redispatch_job` does have user-facing copy, in `CONFIRM.redispatch`.

## Follow-ups
| Action | Owner | Detail |
|---|---|---|
| Decide `[eslint-for-one-rule]` | Human | Filed in `docs/practices/bridge.md`, collected into `docs/OPEN.md`. Paired with `[clippy-as-a-check]` in `rust.md` |
| Install ESLint for `no-floating-promises`, once decided | Fleet | Issue on the board, blocked on the question above |
| Two worktrees left standing | Human | `agent-a86fd01473892161d` is unmerged; `panel` sits at a base commit with no work, which is what a just-started agent looks like |

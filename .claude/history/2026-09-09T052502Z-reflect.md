---
skill: reflect
verdict: PASS
date: 2026-09-09T05:25:02Z
---

One Job that stalled on its first step turned into five pull requests and one
completed Job. `01M21BKVPW002DC0ATD1X9T0VF` failed three times on `scope` for
three different reasons, each a defect in Armada rather than in the work: a
Drone that ended its turn waiting on a subagent whose report could never reach
it (#531); a convergence look shown only a diff, on a step whose product is a
gitignored file, so it could answer nothing but `thrashing` (#540); and a
forced-report grace measured from when Fleet wrote the directive rather than
from when the Drone was handed it, 92 seconds later (#543). The Job then went
over a $5 cap nobody could raise, which turned up the spend figure being wrong
in both directions — a killed Drone billed at nought, and a step move published
before its spend row was written — and a cost cap that now tiers across machine,
`armada.yml` and the Job, with an act to raise it (#556). A read race that let
a stale figure survive the event that should have corrected it followed (#561).

Eight subagents across four waves. The seam between the two budget agents was
pinned before either started — `cost_cap_micros: Option<u64>`, resolution
shaped after `Liveness::at` — and they still both took migration `V32` and both
split `manifest.rs`, resolved at the merge.

Reflect found three gate regressions on `main` that the Job's own PR (#560)
introduced and its gate could not see, because `verify-foundations` is a Command
rather than a Check. Fixed here; the reason it was possible is now an open
question.

## Follow-ups
| Action | Owner | Detail |
|---|---|---|
| Answer `[foundations-as-a-job-gate]` | Human | `docs/contracts/configuration.md`, and in `docs/OPEN.md`. What gates a Job against a run that is legitimately red — the shape has to be a delta against the Job's base, and where that baseline comes from is undecided |
| Delete the pre-migration store backup | Human | `~/Library/Application Support/Armada/armada.db.before-v33`, 284K. Taken before schema 31 → 34; keep it as long as it is useful |

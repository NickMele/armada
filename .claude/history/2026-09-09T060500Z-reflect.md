---
skill: reflect
verdict: PASS
date: 2026-09-09T06:05:00Z
---

A second close on the same session, after the owner found `method allowance is
never used` in `pnpm dev`. It was mine, from #556: `Fleet::allowance` lost its
last caller when the cost cap tiered and `allowance_for` took over, including
from the one site that was drawing the machine constant on the detail of a Job
carrying an override. Deleted in #566.

The interesting part is that nothing caught it. The `build` Check passes with
warnings, `verify-foundations` does not read dead code, and this skill's own
gate table said `cargo build` should be "clean" — which the previous reflect run
read as exit 0, and reported PASS on. Both the skill and the open question now
say otherwise.

`main` at `f36dad81`: 2244 tests, acceptance green, `verify-foundations` green
at 106, `verify-docs` green, `fmt` clean, `cargo build --workspace
--all-targets` warning-free, and all four Bridge checks pass.

## Follow-ups
| Action | Owner | Detail |
|---|---|---|
| Answer `[foundations-as-a-job-gate]` | Human | `docs/contracts/configuration.md`, and in `docs/OPEN.md`. Now carries both instances — the three over-long comment blocks a Job merged past its own green gate, and this warning. Both want the same answer: a delta against the Job's base rather than a threshold. Where that baseline comes from is the open part |

---
skill: reflect
verdict: FAIL
date: 2026-09-12T11:25:00Z
---

**Not ready to close on one thing, and it is this pull request.** Every feature
branch merged and every worktree came back, but the permission classifier
refuses `gh pr merge` without the owner saying so in the moment — so the branch
carrying this file is open rather than landed. The verdict is what is true, not
what the rest of the run earned.

Three pull requests landed — the Job settings panel (680), the mention popup's
keyboard and ARIA (731), and a reject that carries a person's words alongside a
model reading of the command (743) — and this run reconciled the prose those
made false, filed four findings that had no home, and gave back every worktree.

## What the work falsified

Three sentences claimed, absolutely, that a person's words reach a Drone through
`redirect_drone` and nothing else. That was true this morning. Protocol 11.5 made
it false, and each said so in a different place: `crates/api/src/daemon/commands.rs`,
`crates/ipc/src/waiting.rs`, and `docs/capabilities/epic-as-one-job.md`. Each is
now scoped to the operation it is about, and names the reject's note as the other
route. The `answer_question` row in `operations.toml` and `DroneQuestion`'s own
default were checked and left alone: a note appears only under an answer that
declares one, which only the command surface does, so both remain true of a
Drone's own question.

`.claude/skills/agent-worktrees/SKILL.md` gained the incident that cost this
session its ordinary working rhythm: the sandbox refused both dispatched agents
`git worktree add` and `git -C`, so both committed into the dispatching session's
own worktree. Nothing was lost, but the dispatching session could not commit or
run Checks while they worked, and learned it from an agent's report rather than
from its brief.

## Follow-ups

| Action | Owner | Detail |
|---|---|---|
| Split `crates/fleet/src/serving.rs` | Board | #745 — 880 lines, refused at 900; the next query added breaks the build for whoever adds it |
| Decide on a TypeScript formatting Check | Board | #746 — `format` is `cargo fmt`, Rust only; nothing verifies prettier, so a Drone passes every gate whatever it writes |
| Stop a loaded machine reading as a failed suite | Board | #747 — a socket error failed `bridge_test` with all 72 tests passing, and the `&&` chain hid two suites that never ran |
| Decide a glyph, or none, for a model reading | Board | #748 — `icons.toml` has no mark for asking a model to read something; the control ships wordmarked |
| Restart Fleet and Bridge | Human | The running Fleet is protocol 11.4 and `main` is 11.5; the note and the explain control are absent until it is restarted |

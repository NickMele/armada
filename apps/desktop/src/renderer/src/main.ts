// Bridge's renderer entry point. The command centre is designed and not yet
// built; this is the mount point it will attach to.
const root = document.getElementById('root')
if (root) root.textContent = 'Armada'

  STATUS    JOB            DETAIL
  ADVANCED  rate-limit     fix · check_passes — `fix` passed; it is on `land`
  WAITING   bad-parse      reproduce · failing_test_exists — check run 01M00WRY…
  RETRIED   xlsx-report    fix · check_passes — `fix` did not pass (the suite i…
  WORKING   carina-schema  plan — its Drone is still working
  ASKED     release-merge  approval · human_approves — does this look right to …
  HALTED    flaky-suite    review · review_clean — `review` cannot be gated: `r…
  FINISHED  nightly-flake  land · branch_exists — `land` was its last step
  IDLE      port-sweep     implement — it is waiting on you

TICK  4 Jobs moved, 8 looked at

[1m[38;2;255;169;64marmada settings — every setting: its value, where it lives, whether it syncs[0m

[1m[38;2;255;169;64mUSAGE[0m
  armada settings [--json]

[1m[38;2;255;169;64mFLAGS[0m
  --json          [38;2;156;163;175manswer as one JSON object, for a script or an agent[0m
  --color <when>  [38;2;156;163;175mauto (default), always, never; NO_COLOR wins[0m

Read-only. `armada helm enable`/`disable` and the guild's own verbs are
still how a setting changes; this only shows what they left behind.
A setting nobody has touched still gets a row, with its default.

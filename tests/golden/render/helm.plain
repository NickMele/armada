  STATUS     WIRED         DETAIL
  WRITTEN    toolbelt      ~/.armada/helm/mcp.json
  WRITTEN    monitor       ~/.armada/helm/plugin
  WRITTEN    backstop      ~/.armada/helm/stop-inbox.sh
  WRITTEN    voice         ~/.armada/helm/guild-voice.md
  UNCHANGED  conversation  ~/.armada/helm/session.json

  enter with claude --agent helm --append-system-prompt "$(cat ~/.armada/helm/guild-voice.md)" --mcp-config ~/.armada/helm/mcp.json --plugin-dir ~/.armada/helm/plugin --settings ~/.armada/helm/settings.json --permission-mode auto --session-id 15bfa340-33b1-4f81-bd7f-688f0f01dbb0

OK  helm, conversation new, nothing started; entering is off on this machine — `armada helm enable`

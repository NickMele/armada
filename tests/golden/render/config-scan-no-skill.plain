no armada.yml here, this is evidence and not a config

  STATUS  KIND             DETAIL
  FOUND   lockfile         pnpm-lock.yaml, pnpm@9
  FOUND   package scripts  14 in package.json
  FOUND   compose          docker-compose.yml, 3 services
  FOUND   ci               .github/workflows/ci.yml, 6 steps
  ABSENT  pyproject        -
  ABSENT  makefile         -

  package.json scripts, verbatim and not interpreted
    dev         next dev
    build       next build
    test        vitest run
    test:e2e    playwright test
    lint        eslint .
    lint:fix    eslint . --fix
    typecheck   tsc --noEmit
    db:migrate  prisma migrate deploy
    db:seed     prisma db seed
    gen:types   prisma generate
    start       next start
    analyze     ANALYZE=true next build
    clean       rm -rf .next node_modules/.cache
    storybook   storybook dev -p 6006

  docker-compose.yml services
    postgres  5432
    redis     6379
    mailhog   1025, 8025

  ci steps, the best existing evidence of what you actually run
    pnpm install --frozen-lockfile, pnpm lint, pnpm test, pnpm build

  proposals, every line read out of a file, none of it inferred
  WHAT                                     WRITES                 BECAUSE
  components.next-prisma                   the repository itself  package.json
  components.next-prisma.setup             pnpm install           pnpm-lock.yaml
  components.next-prisma.checks.build      pnpm run build         package.json
  components.next-prisma.checks.test       pnpm run test          package.json
  components.next-prisma.checks.lint       pnpm run lint          package.json
  components.next-prisma.checks.typecheck  pnpm run typecheck     package.json

Evidence only. Armada proposes only what a file already said. Nothing is written yet.

  next, no onboarding skill in your guild — `armada guild init`
    claude --append-system-prompt "$(cat ~/.armada/guild/skills/onboard-repo/SKILL.md)" "Onboard this repository for Armada."

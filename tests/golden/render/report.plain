  ID        STATUS  DETAIL                                                  TIME
  6d40b1e9  OPEN    reported, the dry-run said CREATED worktree and made …    0s

  what was reported:
    the dry-run said CREATED worktree and made nothing

  FACT   STATUS     DETAIL
  count  SEEN       once, at 2026-08-09T14:58:11Z
  IN     directory  ~/code/orders
  RAN    armada     0.1.0
  RAN    claude     2.0.14
  ON     system     linux x86_64
  IN     workspace  ~/code/orders, with an armada.yml
  SAID   doctor     STALE guild: 3 commits behind origin
  OPEN   failures   a1b2c3d4 could not be found to run
  IN     flight     orders-fix-a1b2 RUNNING bug

  what was run before it:
  RUN          STATUS  DETAIL                                              TIME
  fleet spawn  OK      armada fleet spawn 'add the report verb' --dry-run    0s
  fleet ls     OK      armada fleet ls                                       1m

  the task a Job would be given:
    This was reported by hand rather than caught by Armada, and was recorded as
    `6d40b1e9`. Armada did not fail: it did something wrong that it did not
    notice, so there may be no error to look for.

    What happened, in the words it was reported in:

        the dry-run said CREATED worktree and made nothing

    Filed on 2026-08-09T14:58:11Z, in ~/code/orders.

    Armada 0.1.0 on linux x86_64, with claude 2.0.14.

    The runs leading up to it, most recent first:

        OK armada fleet spawn 'add the report verb' --dry-run
        OK armada fleet ls

    `armada doctor` on that machine said:

        STALE guild: 3 commits behind origin

    Reproduce what was described, then fix it. `armada failures show` on the id
    above carries the full diagnostics, including each run's envelope.

OK  armada failures fix 6d40b1e9, armada failures clear 6d40b1e9

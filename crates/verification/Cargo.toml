[package]
name = "verification"
version = "0.0.0"
edition.workspace = true
license.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
core-model = { path = "../core-model" }
adapter-traits = { path = "../adapter-traits" }
# The gate reads a step out of a `ResolvedWorkflow`, whose steps carry the
# Check's command rather than its name — so an absent Check is unrepresentable
# here rather than something this crate checks for.
config = { path = "../config" }

[dev-dependencies]
# Fixtures only. `testkit` never ships and is a dependency of nothing that does.
testkit = { path = "../testkit" }

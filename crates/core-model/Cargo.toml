[package]
name = "core-model"
version = "0.0.0"
edition.workspace = true
license.workspace = true
publish = false

[lints]
workspace = true

# Deliberately empty, and checked. `cargo tree` must show no tokio, no git2 and
# no reqwest reachable from here: the model is what every other crate agrees
# on, so anything it pulls in becomes everybody's dependency.
[dependencies]
